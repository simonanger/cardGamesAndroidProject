<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.simonanger.cardgames">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:hardwareAccelerated="false"
        android:largeHeap="true">
        <activity
            android:name=".WelcomeActivity"
            android:label="Card Games">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".HighCardGameActivity"
            android:label="High Card Wins!"
            android:parentActivityName=".WelcomeActivity"/>
        <activity android:name=".BlackJackGameActivity" />
        <activity
            android:name=".CardCountActivity"
            android:parentActivityName=".WelcomeActivity"
            android:label="Card Count!"></activity>
    </application>

</manifest>